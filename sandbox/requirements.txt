# Sandbox Executor Service Dependencies
# КАО#R4-S4sug — keep these pins identical to the common-packages layer in
# sandbox/Dockerfile so the later pip install doesn't silently downgrade them.
fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.1
psutil==5.9.8
